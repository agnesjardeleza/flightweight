<h1>Home</h1>

<%= @posts.each do |x| %>
	Category: <%= x.category %>
	 | posted by 
	 <% if current_user %>
	 <%= user = (User.find(x.poster_id).username == current_user.username) ? 'you' : User.find(x.poster_id).username %>
	 <% else %>
	 <%= User.find(x.poster_id).username %>
	 <% end %>
	 	on <%= x.created_at.to_time.strftime("%b %d, %Y, %I:%M %P") %><br>
	Date of Flight: <%= x.date_of_flight.to_time.strftime("%I:%M %P")%><br>
	<% if x.category == 'request' %>
		Weight Needed: 		
	<% else %>
		Weight Offered: 
	<% end %>
	<%= x.weight %><br><br>
<% end %> 